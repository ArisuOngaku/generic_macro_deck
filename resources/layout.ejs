<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Generic Macro Deck</title>
    <style>
        :root {
            --primary: #37474f;
            --primary-text: #fff;
            --secondary: #0077c0;
            --secondary-text: #fff;
        }

        body {
            margin: 0;
            font-size: 16px;
            font-family: sans-serif;
            color: var(--primary-text);
            background-color: var(--primary);
        }

        button {
            overflow: hidden;
            position: relative;
            padding: 8px 16px;
            color: var(--secondary-text);
            background-color: var(--secondary);
            border-radius: 2px;
            border: 0;
            text-transform: uppercase;
            font-size: 16px;
            box-shadow: 0 1px 2px #00000047;
            cursor: pointer;
        }

        button:hover::before:not([disabled]) {
            content: "";
            display: block;
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: #fff4;
        }

        button[disabled] {
            color: #fffa;
            background-color: #0004;
            box-shadow: none;
            cursor: not-allowed;
        }
    </style>
</head>
<body>
<%- content %>
</body>
</html>