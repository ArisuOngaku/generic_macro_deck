{
  "name": "generic_macro_deck",
  "version": "1.0.0",
  "description": "Turn any keyboard into a macro board",
  "main": "generic_macro_deck.js",
  "type": "module",
  "scripts": {
    "start": "electron generic_macro_deck.js",
    "build": "electron-packager ./ --out build --all",
    "build-arch": "electron-packager ./ --out build --arch=x64 --ignore='^/(.idea|build|.gitignore|build/PKGBUILD|config)'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alice Gaudon",
  "license": "ISC",
  "dependencies": {
    "ejs": "^2.7.1",
    "esm": "^3.2.25",
    "obs-websocket-js": "^3.1.0"
  },
  "devDependencies": {
    "electron": "^6.0.10"
  }
}
